version: '3'

services:
    web:
        image: personal-front
        container_name: personal-front
        restart: always
        build:
            context: .
        ports:
            - 9035:3000
        environment:
            - REACT_APP_API_URL=$REACT_APP_API_URL
